// template.js
module.exports = {
    name: 'template',
    aliases: ['templ', 'platetemp'],
    description: 'template description',
    execute(message) {
        try {
            
        } catch (error) {
            throw error;
        }
    },
};
